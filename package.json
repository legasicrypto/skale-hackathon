{
  "name": "skale-hackathon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy-all.ts --network skale",
    "lint": "echo 'no lint'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "vercel": "^50.15.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
